@EndUserText.label: 'Service for invoice attachments'
define service ZBO_SD_INV_ATTACHMENTS {
  expose ZBO_DD_P_INV_HEAD as Invoice;
  expose ZBO_DD_P_INV_ATTACH as InvoiceAttachments;
  expose /DMO/I_Customer as Customer;
}